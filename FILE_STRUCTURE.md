# File Structure Guide

## ✅ Essential runtime files
- `agents/`
  - Pipeline: `requirements_to_feature_agent.py`, `feature_to_stepdef_agent.py`, `execution_agent.py`, `reporting_agent.py`, `defect_agent.py`
  - Discovery & enrichment: `requirements_aware_ui_discovery_agent.py`, `web_discovery_agent.py`, `ui_context_agent.py`, `xpath_discovery_agent.py`
  - Extraction: `requirements_extraction_agent.py`
- `config.py`, `groq_client.py`, `orchestrator.py`, `behave.ini`
- `requirements.txt`
- `test_system.py` (quick health check)

## ⚙️ Configuration & setup
- `env_template.txt` → copy to `.env` (choose local vs cloud backend; set `BASE_URL` for web runs)
- `bdd.config.yaml` (optional project defaults: `type` = `web` or `api`, `base_url`)
- `setup_and_run.bat` (Windows helper)

## 📚 Documentation in repo
- `README.md`, `SETUP_GUIDE.md`, `HOW_TO_RUN.md`, `ARCHITECTURE.md`, `FILE_STRUCTURE.md`, `TESTING.md`
- `HOW_TO_TEST.txt` (legacy quick notes)
- `WHAT_GETS_SAVED.txt` (artifact locations)

## 🗂️ Generated folders and outputs
- `features/*.feature` (generated)
- `features/steps/*_steps.py` (generated)
- `reports/` (execution & test reports, `ui_locators.properties`)
- `requirements/` (input requirements you create)
- `__pycache__/`, `coverage/` (runtime artifacts)

## 🎯 Minimum files to run
Keep these for a functional pipeline:
- Entire `agents/` directory
- `config.py`, `groq_client.py`, `orchestrator.py`, `behave.ini`, `requirements.txt`
- `.env` (from `env_template.txt`)
- `test_system.py`

## 🧹 What you can clean safely
- Generated artifacts (`features/*.feature`, `features/steps/*_steps.py`, `reports/*`) can be regenerated by rerunning the orchestrator.
- `coverage/` contents are disposable.
